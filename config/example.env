# DealGenie Configuration Examples
# Copy this file to .env and update with your actual values

# ===== WEEK 2 - GEOCODING SERVICE CONFIGURATION =====

# Google Geocoding API (Optional - for enhanced geocoding accuracy)
# Get your API key from: https://console.cloud.google.com/apis/credentials
# GOOGLE_GEOCODING_API_KEY=your_google_geocoding_api_key_here

# Redis Configuration (for caching)
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_TTL=86400

# ===== WEEK 1 - CORE SYSTEM CONFIGURATION =====

# Census API Configuration (Optional - for enhanced demographic data)
# Get your API key from: https://api.census.gov/data/key_signup.html
# CENSUS_API_KEY=your_census_api_key_here

# Database Configuration
DATABASE_URL=sqlite:///./data/dealgenie.db

# For PostgreSQL (Optional):
# DATABASE_URL=postgresql://username:password@localhost:5432/dealgenie
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_NAME=dealgenie
# DATABASE_USER=username
# DATABASE_PASSWORD=password

# ===== PERFORMANCE TUNING =====

# Address Parser Configuration
PARSER_BATCH_SIZE=1000
PARSER_FALLBACK_TO_REGEX=true

# Geocoding Service Configuration
GEOCODER_BATCH_SIZE=10
GEOCODER_MAX_CONCURRENT=5
GEOCODER_REQUEST_TIMEOUT=30
GEOCODER_RETRY_ATTEMPTS=3
GEOCODER_RETRY_DELAY=1.0

# Rate Limiting (requests per second)
NOMINATIM_RATE_LIMIT=1.0
GOOGLE_RATE_LIMIT=50.0

# Circuit Breaker Configuration
CIRCUIT_BREAKER_FAILURE_THRESHOLD=5
CIRCUIT_BREAKER_RECOVERY_TIMEOUT=60
CIRCUIT_BREAKER_EXPECTED_EXCEPTION=RequestException

# ===== DEVELOPMENT & TESTING =====

# Environment
ENVIRONMENT=development  # development, staging, production

# Logging Configuration
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s
LOG_TO_FILE=false
LOG_FILE_PATH=logs/dealgenie.log

# Testing Configuration
TEST_DATA_SIZE=100
TEST_TIMEOUT=30
SKIP_SLOW_TESTS=false

# ===== PRODUCTION DEPLOYMENT =====

# Security
SECRET_KEY=your_secret_key_here
ALLOWED_HOSTS=localhost,127.0.0.1

# Monitoring
HEALTH_CHECK_INTERVAL=300  # seconds
PERFORMANCE_MONITORING=true
METRICS_ENDPOINT=/metrics

# Caching Strategy
CACHE_ENABLED=true
CACHE_DEFAULT_TTL=3600
CACHE_MAX_SIZE=10000

# ===== API CONFIGURATION (FUTURE) =====

# API Server Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_WORKERS=4
API_RELOAD=false

# CORS Configuration
CORS_ALLOW_ORIGINS=*
CORS_ALLOW_METHODS=GET,POST,PUT,DELETE
CORS_ALLOW_HEADERS=*

# ===== EXTERNAL SERVICE TIMEOUTS =====

# HTTP Client Timeouts (seconds)
HTTP_CONNECT_TIMEOUT=10
HTTP_READ_TIMEOUT=30
HTTP_TOTAL_TIMEOUT=60

# Database Connection Pool
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=30
DB_POOL_TIMEOUT=30

# ===== FEATURE FLAGS =====

# Enable/Disable Components
ENABLE_GEOCODING=true
ENABLE_ADDRESS_PARSING=true
ENABLE_CENSUS_INTEGRATION=true
ENABLE_REDIS_CACHE=true
ENABLE_PERFORMANCE_MONITORING=true

# Experimental Features
ENABLE_BATCH_GEOCODING=true
ENABLE_CIRCUIT_BREAKERS=true
ENABLE_ADVANCED_PARSING=false  # Requires libpostal installation

# ===== CONFIGURATION NOTES =====

# 1. This is an example configuration file. Do not commit actual API keys or passwords.
# 2. Copy this file to .env in the project root for local development
# 3. For production, use environment variables or secure secret management
# 4. All timeout values are in seconds unless specified otherwise
# 5. Rate limits are per second unless specified otherwise
# 6. Cache TTL values are in seconds (86400 = 24 hours)